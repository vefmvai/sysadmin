# Вклад в проект

Спасибо, что хочешь улучшить этот проект! Здесь — всё что нужно знать.

## Как сообщить об ошибке (простой путь, в том числе для вайбкодера)

Не обязательно быть айтишником. Два шага:

1. **Собери диагностику автоматически** — не вручную. Самое простое — попроси самого
   агента: **«собери диагностику для баг-репорта»** (он запустит скрипт). Или сам:
   ```bash
   bash scripts/collect-diagnostics.sh
   ```
   Он печатает обезличенный блок (VERSION, ОС, shell, jq, python3, git-коммит, статус
   линтера) — **без секретов, токенов и IP**, можно вставлять в публичный issue.
2. **Открой issue:** <https://github.com/vefmvai/sysadmin/issues> (есть готовый шаблон
   «Баг-репорт» — он подскажет, что заполнить). Заголовок по сути (`/sysadmin-init падает
   на macOS Sonoma`), в теле: что делал / что ожидал / что получил + вставь блок диагностики.

**Никогда не прикладывай** секреты, токены, пароли, приватные ключи, реальные IP серверов.
Если агент сказал «настройка не завершена, свяжись с разработчиком» — это и есть приглашение
открыть issue по ссылке выше с приложенной диагностикой.

## Как предложить улучшение

1. Форкни репозиторий: `git fork` или кнопка Fork на GitHub.
2. Создай ветку: `git checkout -b feat/название-улучшения`.
3. Внеси изменения, сделай коммит (см. ниже стиль коммитов).
4. Открой Pull Request с описанием: что изменил и зачем.

## Стиль коммитов

Формат: `тип(область): описание на русском`

**Допустимые типы:**

| Тип | Когда использовать |
|-----|-------------------|
| `feat` | Новая функциональность |
| `fix` | Исправление ошибки |
| `docs` | Изменения в документации |
| `chore` | Настройка инструментов, конфигов |
| `refactor` | Рефакторинг без изменения поведения |

**Примеры:**
```
feat(skills): добавлен новый скилл /restore-from-backup
fix(skills): исправлен путь в /sysadmin-init на macOS
docs(readme): уточнено описание /sysadmin-meet
```

## Краткая структура проекта

```
CLAUDE.md                    — ★ ЯДРО ПЕРСОНЫ агента (вход 2.0, ADR-0015): ценности,
                               конституция, характер, протоколы, три зоны
.claude/agents/references/   — углублённые протоколы персоны (trust-zones, cold-start, …)
.claude/agents/sysadmin.md   — тонкая заглушка-указатель на CLAUDE.md (для @sysadmin из чужих папок)
.claude/skills/              — 21 операционный скилл + /dev (готовые процедуры)
decisions/0000-template.md   — шаблон ADR для твоих собственных решений
README.md                    — главный документ репо: что это, как начать
CONTRIBUTING.md              — этот файл
LICENSE                      — лицензия (MIT)
agent-config.example.json    — публичный шаблон конфига МОЗГА (обезличенный)
agent-config.schema.json     — JSON Schema 2020-12, контракт конфига мозга
agent-config.json            — твой личный конфиг мозга (в .gitignore, создаётся /sysadmin-init)
infra-config.example.json    — публичный шаблон конфига КАРТЫ инфры (обезличенный)
infra-config.schema.json     — JSON Schema 2020-12, контракт конфига карты
                               (личный infra-config.json живёт в папке проекта infra/, в .gitignore)
```

## Правило безопасности

**Никогда не коммить:**
- Пароли, токены, API-ключи
- Реальные IP-адреса чужих серверов
- SSH-ключи (приватные)
- `.env`-файлы с реальными значениями

**Проверка на секреты уже встроена в репозиторий** — руками хук писать не нужно.
`.githooks/pre-commit` прогоняет `gitleaks` по staged-изменениям на каждом коммите
и блокирует его при находке. Что требуется от тебя:

1. **Поставить gitleaks** — [инструкция по установке](https://github.com/gitleaks/gitleaks#installing)
   (`brew install gitleaks`, пакет из дистрибутива или бинарник с GitHub releases).
   Проверка **fail-closed**: если gitleaks не найден, коммит отклоняется. Это намеренно —
   «предупредил и пропустил» создаёт ложное чувство защиты.
2. **Активировать хук один раз после клонирования:** `git config core.hooksPath .githooks`
   (скилл `/sysadmin-init` делает это сам).

Правила поиска — `.gitleaks.toml` в корне: стандартный набор gitleaks плюс токен
Telegram-бота, который базовый набор **не ловит** (проверено на 8.30.1). Ложное
срабатывание — добавь точечное исключение в `.gitleaks.toml`, а не отключай проверку.
Если хук сработал по делу — убери секрет из коммита, не обходи через `--no-verify`.

## Ещё раз: спасибо!

Любой вклад — даже правка опечатки — ценен. Этот проект создан для тех, кто хочет
навести порядок в своей инфраструктуре. Твой опыт поможет следующим.
