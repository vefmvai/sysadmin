# Ротация секрета `<SECRET_NAME>` — YYYY-MM-DD

**Тип:** db-password / api-token / ssh-key / tls-cert / oauth-secret
**Причина:** scheduled / compromised / manual
**Кто запускал:** sysadmin (под наблюдением оператора)
**Длительность:** N минут (от первого ALTER до последнего verify)
**Downtime:** N секунд (если был)

---

## Pre-state (до ротации)

- Дата последней ротации: `YYYY-MM-DD`
- Причина текущей ротации: `<scheduled — подошла плановая дата | compromised — утечка
  обнаружена в <место> | manual — оператор инициировал>`
- Потребители секрета:
  - `/opt/<service-1>/.env`
  - `/opt/<service-2>/.env`
  - `/etc/cron.d/<job>`

## Что было сделано

1. Сгенерирован новый секрет (длина / алгоритм).
2. Применён в БД / провайдерской панели.
3. Подменён во всех потребителях через `sed -i.bak`.
4. Restart всех потребителей: `<список compose-проектов>`.
5. Verify нового — PASS.
6. Verify старого не работает — PASS.
7. Старый удалён из менеджера паролей.

Команды (или ссылка на запущенный скрипт `scripts/rotate-db-password.sh ...`):

```bash
# вставить актуальные команды из сессии
```

## Что пошло не так (если что-то)

- (Если ничего — написать «Без инцидентов».)
- (Если было — описать что именно, как восстанавливался работоспособный
  state.)

## Урок

(1-3 предложения. Что выяснилось в процессе ротации, что улучшить в следующий раз.
Например: «Обнаружили потребителя `<service-3>` в /etc/cron.d/, которого не было в
первоначальном списке — обновить чек-лист»; или «Restart `<service>` занял 30 сек
вместо 5 — это в норме для приложения с pool'ом, но превышает заявленный downtime»).

## Метаданные

- access.md обновлён: `<да>`
- следующая ротация: `YYYY-MM-DD` (через `<период>` по
  `references/rotation-schedule.md`)
- backup перед ротацией: `<тег restic / нет — не требовался для этого типа>`